import { createBrowserRouter } from "react-router-dom";
import { routeConfiguration } from "../routeConfiguration/routeConfiguration";

export const appRouter = createBrowserRouter(routeConfiguration);
